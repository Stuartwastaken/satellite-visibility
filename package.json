{
  "name": "starlink-visualizer",
  "version": "1.0.0",
  "description": "Starlink Constellation Visualizer with C++ Backend",
  "scripts": {
    "build:cpp": "mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make visualizer_data && ./visualizer_data",
    "build": "npm run build:cpp",
    "start": "cd visualizer && npx serve ."
  },
  "keywords": ["spacex", "starlink", "cpp", "threejs"],
  "author": "Stuart Ray",
  "license": "MIT"
}
